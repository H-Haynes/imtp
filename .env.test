# 测试环境配置
NODE_ENV=test
DEBUG=false
PORT=3001
HOST=localhost

# 测试环境数据库配置
DATABASE_URL=postgresql://test_user:test_password@localhost:5432/imtp_test
DATABASE_NAME=imtp_test

# 测试环境 Redis 配置
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379

# 测试环境 JWT 配置
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# 测试环境 API 配置
API_BASE_URL=http://localhost:3001/api
API_DOCS_URL=http://localhost:3001/api-docs
API_TIMEOUT=3000
API_TYPES_OUTPUT=packages/types/src/api/generated/openapi.ts

# 测试环境文件上传配置
UPLOAD_MAX_SIZE=1048576
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png

# 测试环境邮件配置
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=test_user
SMTP_PASS=test_password

# 测试环境第三方服务
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret

# 测试环境监控配置
SENTRY_DSN=
LOG_LEVEL=error
